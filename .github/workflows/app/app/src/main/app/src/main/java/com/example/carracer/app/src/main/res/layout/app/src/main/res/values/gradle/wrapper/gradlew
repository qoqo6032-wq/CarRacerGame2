#!/bin/sh
# Gradle wrapper startup script

DIR="$( cd "$( dirname "$0" )" && pwd )"
GRADLE_WRAPPER_JAR="$DIR/gradle/wrapper/gradle-wrapper.jar"

exec java -Xmx64m -cp "$GRADLE_WRAPPER_JAR" org.gradle.wrapper.GradleWrapperMain "$@"
